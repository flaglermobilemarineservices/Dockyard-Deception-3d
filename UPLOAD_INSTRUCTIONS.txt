Dockyard Deception v18.7 — Chas NPC + Clean HUD

Changes:
- Added Chas as an ambient dock NPC using the supplied Meshy walk and idle animations.
- Chas wanders the safe dock graph, pauses naturally, avoids Rick and Gage, and is tagged for reuse as a future client.
- Removed the old top-left training/step/status box from view.
- Keeps the full v18.6 3D engine-oil service.

Upload every file in the full package to the repo root, replacing matching files.
For an existing v18.6 install, the update package only needs index.html, chas_walk.glb, chas_idle.glb.
